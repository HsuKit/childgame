const points = ['段落阅读', '常见时态', '情境交际', '指代理解', '简单推断', '信息整理', '功能表达', '真实任务']
const distribution = [
  ['choice', 1, 52], ['choice', 2, 37], ['choice', 3, 16],
  ['fill', 1, 11], ['fill', 2, 7], ['fill', 3, 3],
  ['match', 1, 7], ['match', 2, 5], ['match', 3, 2],
]

const records = [
  { name: 'Mia', place: 'school garden', activity: 'measured the bean plants', time: 'last Friday', next: 'will compare the new leaves tomorrow', object: 'the tallest plant', pronoun: 'it', reason: 'to learn how sunlight changes plant growth', result: 'the sunny group grew more leaves', task: 'complete a growth chart' },
  { name: 'Leo', place: 'community library', activity: 'sorted returned books', time: 'yesterday afternoon', next: 'will design shelf labels next week', object: 'the history shelf', pronoun: 'it', reason: 'to help younger readers find books', result: 'readers found the story area faster', task: 'make a clear library map' },
  { name: 'Emma', place: 'science room', activity: 'tested three paper bridges', time: 'on Tuesday', next: 'will strengthen the best design tomorrow', object: 'the folded bridge', pronoun: 'it', reason: 'to discover which shape holds more coins', result: 'the folded bridge held the most coins', task: 'write a fair-test report' },
  { name: 'Noah', place: 'sports field', activity: 'recorded four running times', time: 'last Monday', next: 'will practise a steady pace this weekend', object: 'the second lap', pronoun: 'it', reason: 'to improve his pacing plan', result: 'his final two laps were more even', task: 'prepare a training timetable' },
  { name: 'Sophie', place: 'school kitchen', activity: 'weighed food waste', time: 'after lunch yesterday', next: 'will share smaller-serving advice on Friday', object: 'the waste box', pronoun: 'it', reason: 'to reduce unfinished meals', result: 'the soup group left the least food', task: 'create a food-saving notice' },
  { name: 'Ethan', place: 'local museum', activity: 'sketched an old wooden window', time: 'during Saturday’s visit', next: 'will add notes about its patterns tonight', object: 'the window sketch', pronoun: 'it', reason: 'to explain a traditional building style', result: 'his group noticed repeated flower shapes', task: 'give a two-minute exhibit talk' },
  { name: 'Ava', place: 'wetland centre', activity: 'counted birds from a quiet path', time: 'early Sunday morning', next: 'will check another observation record later', object: 'the bird list', pronoun: 'it', reason: 'to avoid disturbing the animals', result: 'more birds appeared after the group became quiet', task: 'organise an observation table' },
  { name: 'Daniel', place: 'music room', activity: 'recorded the class rhythm practice', time: 'before school today', next: 'will mark the difficult bars this evening', object: 'the sound file', pronoun: 'it', reason: 'to help the group practise accurately', result: 'the third part needed a slower beat', task: 'send useful practice feedback' },
  { name: 'Chloe', place: 'bus station', activity: 'checked two weekend routes', time: 'on Wednesday evening', next: 'will confirm the first-bus time tomorrow', object: 'the route card', pronoun: 'it', reason: 'to plan a museum trip', result: 'Route 6 required one fewer change', task: 'choose a route for the group' },
  { name: 'Jack', place: 'recycling centre', activity: 'interviewed a volunteer', time: 'last Thursday', next: 'will turn the notes into a poster next week', object: 'the interview note', pronoun: 'it', reason: 'to explain correct waste sorting', result: 'many mistakes involved dirty containers', task: 'make a sorting guide' },
  { name: 'Lily', place: 'weather station', activity: 'read the rain gauge', time: 'at eight this morning', next: 'will compare the weekly totals on Sunday', object: 'the rainfall table', pronoun: 'it', reason: 'to report changes in local rainfall', result: 'Wednesday had the highest reading', task: 'present a weekly weather summary' },
  { name: 'Owen', place: 'school hall', activity: 'tested the microphone positions', time: 'after class yesterday', next: 'will place floor marks before the show', object: 'the centre position', pronoun: 'it', reason: 'to make every speaker easy to hear', result: 'the centre position produced the clearest sound', task: 'write instructions for speakers' },
  { name: 'Ruby', place: 'neighbourhood park', activity: 'surveyed visitors about shade', time: 'on a warm afternoon', next: 'will group the answers by age tomorrow', object: 'the survey form', pronoun: 'it', reason: 'to suggest better resting areas', result: 'families preferred seats near trees', task: 'prepare an evidence-based suggestion' },
  { name: 'Henry', place: 'art classroom', activity: 'photographed each stage of printmaking', time: 'during Friday’s workshop', next: 'will arrange the photos in order tonight', object: 'the final print', pronoun: 'it', reason: 'to teach another class the process', result: 'careful alignment made the colours clearer', task: 'build a step-by-step display' },
]

function choice(stem, correct, wrong, position, explanation) { const options = [...wrong]; options.splice(position, 0, correct); return { stem, options, answer: position, explanation } }
function sentence(text) { return `${text[0].toUpperCase()}${text.slice(1)}` }
function paragraph(r) { return `${sentence(r.time)}, ${r.name} ${r.activity} at the ${r.place} ${r.reason}. ${sentence(r.result)}. ${r.name} ${r.next}.` }

function basicChoice(point, r, position) {
  if (point === '段落阅读') return choice(`Read: “${paragraph(r)}” What is the paragraph mainly about?`, `${r.name}'s study task and what the record showed`, ['a fictional trip with no plan', 'a meal with no information', 'a list of unrelated colours'], position, 'The paragraph connects a task, its purpose, a result and a next step.')
  if (point === '常见时态') return choice(`The note says, “${sentence(r.time)}, ${r.name} ${r.activity}.” Which phrase shows a completed past action?`, r.activity, [r.next, r.reason, r.task], position, `The past-time phrase and “${r.activity}” show a completed action.`)
  if (point === '情境交际') return choice(`${r.name} needs help with the task “${r.task}”. Which request is clear and polite?`, `Could you please help me ${r.task}?`, ['Do it now because I say so!', 'What colour is yesterday?', 'I will not explain the task.'], position, '“Could you please...?” makes a polite request and names the needed task.')
  if (point === '指代理解') return choice(`Read: “${r.name} checked ${r.object} and then wrote about ${r.pronoun} in the project folder.” What does “${r.pronoun}” refer to?`, r.object, [r.name, r.place, r.reason], position, `The pronoun refers back to the singular noun phrase “${r.object}”.`)
  if (point === '简单推断') return choice(`Fact 1: ${r.name} worked ${r.reason}. Fact 2: ${r.result}. What can we reasonably infer?`, `The task produced information useful for its purpose.`, ['The result proves every future case.', 'No observation was made.', 'The place must close forever.'], position, 'The purpose and recorded result are connected, but the evidence does not prove every possible case.')
  if (point === '信息整理') return choice(`Project card: “Who: ${r.name}; where: ${r.place}; result: ${r.result}.” Which note keeps all three fields?`, `${r.name} — ${r.place} — ${r.result}`, [`another pupil — airport — no result`, `${r.name} — wrong place — no record`, `nobody — nowhere — nothing`], position, 'The correct note preserves the person, place and result.')
  if (point === '功能表达') return choice(`${r.name} reports, “${r.result}.” Which sentence best introduces this evidence?`, `According to our record, ${r.result}.`, ['I have no evidence, but I am always right.', 'Please forget every result.', 'This number is a colour.'], position, '“According to our record” clearly introduces evidence from collected information.')
  return choice(`${r.name} must ${r.task}. Which first step is most useful?`, `Select key facts from the activity record, including that ${r.result}`, ['Remove the purpose and all evidence', 'Copy an unrelated story', 'Choose a design before reading the information'], position, 'A real task should begin by selecting information that supports its purpose.')
}

function appliedChoice(point, r, position) {
  if (point === '段落阅读') return choice(`Read the project log: “${paragraph(r)}” Which title includes both the investigation and its value?`, `${r.name}'s evidence for the task “${r.task}”`, ['A day with no action or result', 'A guide to an unrelated airport', 'Why records should always be deleted'], position, 'A strong title represents both what was done and how the result will be used.')
  if (point === '常见时态') return choice(`Log: “${r.name} ${r.activity} ${r.time}. ${r.name} ${r.next}.” Which description of time is correct?`, 'The first action is completed; the second is planned.', ['Both actions happen every minute.', 'Both actions finished before the past record.', 'The second sentence has no future meaning.'], position, 'The past form reports completed work; “will” reports a future plan.')
  if (point === '情境交际') return choice(`${r.name} presents “${r.result}”, but a classmate does not understand how it supports the task. Which reply is most helpful?`, `Let me explain how this result helps us ${r.task}.`, ['You must agree without a reason.', 'The result has nothing to do with our work.', 'I will hide the record.'], position, 'The reply offers a clear explanation linking evidence to the shared task.')
  if (point === '指代理解') return choice(`Read: “${r.name} described ${r.object} in the report because ${r.pronoun} was important to the project.” Which noun does “${r.pronoun}” replace, and why?`, `${r.object}, because it is the item described as important`, [`${r.name}, because a person is a folder`, `${r.place}, because every place is paper`, `${r.reason}, because a purpose is an object`], position, 'Grammar and meaning both point to the singular item immediately discussed.')
  if (point === '简单推断') return choice(`The log records “${r.result}”. ${r.name} ${r.next}. Which inference is best supported?`, 'The next step will use or check information from the earlier result.', ['The result can never change with more evidence.', 'The project has no purpose.', 'The next step happened many years ago.'], position, 'The future step logically follows the collected result, while certainty beyond the evidence is not justified.')
  if (point === '信息整理') return choice(`Two notes must be merged. A: “${r.name}; ${r.activity}; ${r.time}.” B: “Result: ${r.result}; next: ${r.next}.” Which summary loses no key field?`, `${r.name} completed the activity ${r.time}, recorded that ${r.result}, and ${r.next}`, ['Only the pupil’s name is known.', 'The activity had no time, result or next step.', 'A different person did an opposite task.'], position, 'The correct summary preserves actor, completed action, time, result and next step.')
  if (point === '功能表达') return choice(`${r.name}'s record says “${r.result}” and the project task is to ${r.task}. Which sentence clearly separates evidence from a suggestion?`, `Our record shows that ${r.result}; therefore, we suggest using it to ${r.task}.`, ['The suggestion is a fact because I like it.', 'There is no difference between data and opinion.', 'Delete the result before making a suggestion.'], position, '“Our record shows” introduces evidence; “therefore, we suggest” introduces a recommendation.')
  return choice(`For the task “${r.task}”, which plan follows a useful order?`, `identify the audience → select evidence → organise it → check whether the task is clear`, ['decorate first → remove evidence → ignore the audience → stop', 'copy everything → mix the order → hide the purpose → guess', 'choose an answer → avoid reading → change the task → delete'], position, 'A real communication task begins with audience and evidence, then organises and checks the result.')
}

function hardChoice(point, r, position) {
  if (point === '段落阅读') return choice(`Read: “${paragraph(r)}” Which outline best represents the paragraph’s reasoning?`, 'completed action → observed result → future use or check', ['future claim → no action → unrelated ending', 'place name → colour list → imaginary meal', 'answer first → remove evidence → change the subject'], position, 'The paragraph moves from completed work to evidence and then to a planned next step.')
  if (point === '常见时态') return choice(`Which rewrite keeps the time order in this log: “${r.name} ${r.activity} ${r.time} and ${r.next}”?`, `After completing the recorded work, ${r.name} plans the stated next step.`, ['The future step caused an action that never happened.', 'Both actions are permanent facts with no time.', 'The past action will begin after the future step is finished yesterday.'], position, 'The completed action comes first and the future plan follows it.')
  if (point === '情境交际') return choice(`While ${r.name} is working on the task “${r.task}”, a listener hears “${r.result}” and says, “One result may not be enough.” Which reply is polite and evidence-aware?`, `That is a fair point. We can check more records before making a stronger claim.`, ['Be quiet; one result proves everything.', 'Evidence is not needed in a project.', 'I will change the numbers to win.'], position, 'The reply respects the concern and proposes gathering more evidence.')
  if (point === '指代理解') return choice(`In “${r.name} described ${r.object} after checking the report because ${r.pronoun} was needed to ${r.task},” what evidence resolves the pronoun?`, `singular grammar and the meaning that ${r.object} can support the task`, ['the person’s name alone', 'the place is always the nearest word', 'all nouns must have the same meaning'], position, 'Reliable reference uses both grammatical agreement and sentence meaning.')
  if (point === '简单推断') return choice(`Evidence: “${r.result}.” Purpose: “${r.reason}.” Which conclusion is careful enough for the evidence?`, 'The result supports a useful possibility, but more records may strengthen the conclusion.', ['The result proves the same outcome everywhere forever.', 'The evidence should be ignored because it is not perfect.', 'The purpose automatically makes every claim true.'], position, 'A careful inference uses the evidence without claiming more certainty than it provides.')
  if (point === '信息整理') return choice(`To build the task “${r.task}”, which field order makes the evidence easiest to check?`, `question or purpose → method and time → result → next action`, ['answer → hidden method → no result → decoration', 'name only → unrelated joke → repeated title → blank', 'future action → changed numbers → unknown source → guess'], position, 'This order shows why information was collected, how it was collected, what it showed and what follows.')
  if (point === '功能表达') return choice(`Which wording reports “${r.result}” without presenting it as an unlimited fact?`, `In this record, ${r.result}.`, [`This proves that ${r.result} in every place and time.`, `Everyone knows it, so no source is needed.`, `The opposite result is true because we prefer it.`], position, '“In this record” correctly limits the claim to the available evidence.')
  return choice(`Before ${r.name} finishes “${r.task}”, which check best tests whether the product is trustworthy and usable?`, `Can the audience trace each main claim to the record and follow the intended action?`, ['Is every fact hidden behind decoration?', 'Does it avoid naming any source or purpose?', 'Is it long even when the order is unclear?'], position, 'A trustworthy product makes claims traceable and gives its audience a clear path to use the information.')
}

const fills = [
  ['段落阅读', 'A paragraph’s main idea connects its important details, not just one small ___.', 'detail'], ['常见时态', 'Yesterday we ___ the results.（填 record 的过去式）', 'recorded'],
  ['情境交际', 'To disagree politely, say “I understand, ___ I see it differently.”', 'but'], ['指代理解', 'In “The map is useful, so keep it,” “it” refers to the ___.', 'map'],
  ['简单推断', 'A careful inference is based on evidence, not a wild ___.', 'guess'], ['信息整理', 'A table can organise time, method and ___.', 'result'],
  ['功能表达', '“According to the survey” introduces collected ___.', 'evidence'], ['真实任务', 'Before writing a notice, think about its audience and ___.', 'purpose'],
  ['段落阅读', 'Words such as “however” may signal a change or ___.', 'contrast'], ['常见时态', 'Tomorrow we ___ check the second record.', 'will'],
  ['情境交际', '“Could you explain that again, ___?” completes a polite request.', 'please'], ['指代理解', 'A pronoun must agree in meaning and number with the noun it ___.', 'replaces'],
  ['简单推断', 'One example may support an idea but may not ___ it for every case.', 'prove'], ['信息整理', 'A useful summary keeps key facts and removes ___ details.', 'unrelated'],
  ['功能表达', 'Use “therefore” to introduce a result or ___.', 'conclusion'], ['真实任务', 'A report should let readers trace a claim back to its ___.', 'source'],
  ['段落阅读', 'The relationship among sentences forms the paragraph’s ___.', 'logic'], ['常见时态', 'She has ___ the chart already.（填 finish 的过去分词）', 'finished'],
  ['简单推断', 'When evidence is limited, use careful words such as “may” or “___”.', 'might'], ['信息整理', 'To compare records fairly, keep the same categories and ___.', 'units'],
  ['真实任务', 'After drafting a guide, test whether another reader can ___ it.', 'use'],
]

const matchSets = [
  ['段落阅读', ['main idea', 'supporting detail', 'title', 'paragraph order'], ['central message', 'specific evidence', 'short topic label', 'logic between sentences']],
  ['常见时态', ['worked', 'works', 'is working', 'will work'], ['completed past', 'regular present', 'happening now', 'future plan']],
  ['情境交际', ['Could you help?', 'I see your point.', 'Could you repeat that?', 'Thank you.'], ['request help', 'acknowledge an idea', 'ask for clarification', 'show thanks']],
  ['指代理解', ['he', 'she', 'it', 'they'], ['one male person', 'one female person', 'one thing', 'several people or things']],
  ['简单推断', ['fact', 'clue', 'inference', 'overclaim'], ['direct information', 'supporting sign', 'reasoned conclusion', 'claim beyond evidence']],
  ['信息整理', ['who', 'when', 'method', 'result'], ['person', 'time', 'how work was done', 'what the record showed']],
  ['功能表达', ['according to', 'however', 'therefore', 'for example'], ['introduce source', 'show contrast', 'show conclusion', 'give an instance']],
  ['真实任务', ['audience', 'purpose', 'evidence', 'review'], ['intended reader', 'reason for creating', 'supporting information', 'final check']],
]

function englishMatch(serial) {
  const [point, left, answers] = matchSets[(serial - 126) % matchSets.length], order = [2, 0, 3, 1], right = order.map(i => answers[i]), lookup = Object.fromEntries(order.map((x, i) => [x, i]))
  const prompts = { 段落阅读: 'Match each paragraph-reading term with its job.', 常见时态: 'Match each verb form with its time meaning.', 情境交际: 'Match each polite sentence with its purpose.', 指代理解: 'Match each pronoun with what it can replace.', 简单推断: 'Match each reasoning term with its meaning.', 信息整理: 'Match each record field with the information it stores.', 功能表达: 'Match each linking expression with its function.', 真实任务: 'Match each project element with the question it answers.' }
  return { point, content: { stem: `${serial < 134 ? 'Use the project language chart.' : 'Review the communication checklist.'} ${prompts[point]}`, left, right, matches: left.map((_, i) => [i, lookup[i]]), explanation: `Compare the meaning and function of all four ${point} items.` } }
}

export function authorGrade5English() {
  const questions = []; let serial = 0
  for (const [type, difficulty, count] of distribution) for (let index = 0; index < count; index += 1) {
    let point = points[serial % points.length], content
    if (type === 'choice') { const r = records[Math.floor(serial / points.length)]; content = difficulty === 1 ? basicChoice(point, r, serial % 4) : difficulty === 2 ? appliedChoice(point, r, serial % 4) : hardChoice(point, r, serial % 4) }
    else if (type === 'fill') { const [p, stem, answer] = fills[serial - 105]; point = p; content = { stem, answer, explanation: `The word “${answer}” completes the sentence accurately.` } }
    else { const item = englishMatch(serial); point = item.point; content = item.content }
    questions.push({ id: `g5-english-authored-${String(serial + 1).padStart(3, '0')}`, subject: 'english', grade: 5, difficulty, type, knowledgePoint: point, skill: difficulty === 1 ? 'understand' : difficulty === 2 ? 'apply' : 'reason', tags: ['全国通用', difficulty > 1 ? 'evidence-based reading' : 'project English'], content, reviewStatus: 'reviewed', version: 1 }); serial += 1
  }
  return questions
}
